export const environment = {
  production: true
};

/* => que es de production , => solamente tener una url que apunte al servidor real y angular va hacer todo para nosotros cuando lo compilemos a la production   */
